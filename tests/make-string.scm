(display "[")
(display (make-string 0)); empty string
(display "]\n")

(display "[")
(display (make-string 5)); unspeficied, let's use space
(display "]\n")

(display "[")
(display (make-string 0 #\q)); again, empty string
(display "]\n")

(display "[")
(display (make-string 5 #\q)); "qqqqq"
(display "]\n")

